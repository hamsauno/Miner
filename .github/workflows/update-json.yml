name: Обновление файлов при изменениях

on:
  push:
    branches:
      - main  # Здесь указывается ветка, на которую происходит push
    paths:
      - '**/*'  # Это отслеживает все изменения в репозитории (все файлы и папки)

jobs:
  update-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout репозиторий
        uses: actions/checkout@v2

      - name: Установить Node.js (если нужно)
        uses: actions/setup-node@v2
        with:
          node-version: '14'  # Задайте версию Node.js, если нужно

      - name: Запуск обновления всех файлов
        run: |
          # Здесь вы можете указать команды для выполнения, например:
          echo "Обновление файлов успешно выполнено"
